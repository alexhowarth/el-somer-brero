# Runtime dependency — present in the Lambda runtime automatically, but needed
# locally so that `import boto3` in app.py succeeds and can be mocked in tests.
boto3>=1.34

# Test runner.
pytest>=8.0

# Coverage plugin — lets pytest measure which lines of src/ are exercised.
pytest-cov>=5.0

# All-in-one linter + formatter.
# Replaces flake8, pyflakes, pycodestyle, isort, and pyupgrade in a single tool.
ruff>=0.4
